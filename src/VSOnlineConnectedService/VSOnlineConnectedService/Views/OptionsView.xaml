<UserControl x:Class="VSOnlineConnectedService.Views.OptionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VSOnlineConnectedService.Views"
             xmlns:viewModels="clr-namespace:VSOnlineConnectedService.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewModels:OptionsViewModel}"
             mc:Ignorable="d" 
             d:DesignWidth="584" d:DesignHeight="367" >
    <StackPanel>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Label Grid.Row="1" Grid.Column="0" Content="Service Name:" />
            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding ServiceName}"/>
        </Grid>
    </StackPanel>
</UserControl>
